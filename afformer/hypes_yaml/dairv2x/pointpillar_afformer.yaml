name: pointpillar_afformer
data_dir: "dataset/dairv2x"
root_dir: "dataset/dairv2x/train.json"
validate_dir: "dataset/dairv2x/val.json"
test_dir: "dataset/dairv2x/val.json"

num_sweep_frames: &k 2

channel_params:
  SNR_fix: False
  SNR: -10.0
  freq_GHz: 5.9                  # 5.9 GHz (ITS-G5 / NR-V2X)
  bandwidth_Hz: 10e6              # 10 MHz
  Pt_dBm: 23.0        # 200 mW
  Gt_cav_dBi: 3  
  Gt_rsu_dBi: 6                 # Transmitter antenna gain
  Gr_dBi: 3                   # Receiver antenna gain
  NF_dB: 6                      # Noise figure


yaml_parser: "load_point_pillar_params"
train_params:
  batch_size: &batch_size 2
  epoches: 300
  patience: 10
  max_cav: &max_cav 2

input_source: ['lidar']
label_type: 'lidar'

comm_range: 100

fusion:
  core_method: 'intermediate' # late, early, intermediate supported
  dataset: 'dairv2x'
  args: [ ]


data_augment:
  - NAME: random_world_flip
    ALONG_AXIS_LIST: [ 'x' ]

  - NAME: random_world_rotation
    WORLD_ROT_ANGLE: [ -0.78539816, 0.78539816 ]

  - NAME: random_world_scaling
    WORLD_SCALE_RANGE: [ 0.95, 1.05 ]

# preprocess-related
preprocess:
  # options: BasePreprocessor, VoxelPreprocessor, BevPreprocessor
  core_method: 'SpVoxelPreprocessor'
  args:
    voxel_size: &voxel_size [0.4, 0.4, 5]
    max_points_per_voxel: 32
    max_voxel_train: 32000
    max_voxel_test: 70000
  # lidar range for each individual cav.
  cav_lidar_range: &cav_lidar [-100.8, -40, -3.5, 100.8, 40, 1.5]


# anchor box related
postprocess:
  core_method: 'VoxelPostprocessor' # VoxelPostprocessor, BevPostprocessor supported
  gt_range: *cav_lidar
  anchor_args:
    gt_range: *cav_lidar
    cav_lidar_range: *cav_lidar
    l: 4.5
    w: 2
    h: 1.56
    r: &anchor_yaw [0, 90]
    feature_stride: 2
    num: &achor_num 2
  target_args:
    pos_threshold: 0.6
    neg_threshold: 0.45
    score_threshold: 0.2
  order: 'hwl' # hwl or lwh
  max_num: 100 # maximum number of objects in a single frame. use this number to make sure different frames has the same dimension in the same batch
  nms_thresh: 0.15
  dir_args: &dir_args
    dir_offset: 0.7853
    num_bins: 2
    anchor_yaw: *anchor_yaw

# model related
model:
  core_method: point_pillar_tomv_kd
  args: &model_args
    voxel_size: *voxel_size
    lidar_range: *cav_lidar
    anchor_number: *achor_num
    compression: 64
    max_cav: *max_cav
    seq_len: *k

    fading: False
    backbone_fix: false
    encoder_type: basic
      
    pillar_vfe:
      use_norm: true
      with_distance: false
      use_absolute_xyz: true
      num_filters: [64]
    point_pillar_scatter:
      num_features: 64

    base_bev_backbone:
      resnet: true
      layer_nums: &layer_nums [3, 4, 5]
      layer_strides: [2, 2, 2]
      num_filters: &num_filters [64, 128, 256]
      upsample_strides: [1, 2, 4]
      num_upsample_filter: [128, 128, 128]
      compression: 0
      voxel_size: *voxel_size

    shrink_header:
      kernal_size: [3]
      stride: [1]
      padding: [1]
      dim: [256]
      input_dim: 384 # 128 * 3
    
    dir_args: *dir_args


    transformer:
      dim_in: 256
      dim_out: 64
      # agent-wise attention
      cav_att_config: &cav_att_config
        # number of encoder layers
        depth: 1
        # dimension of the input feature
        dim: 256
        # epsilon for LayerNorm
        eps: 1e-05
        heads: 8
        dim_head: 32
        dropout: 0.3
      temporal_att_config:
        # number of encoder layers
        depth: 1
        # dimension of the input feature
        dim: 256
        # epsilon for LayerNorm
        eps: 1e-05
        heads: 8
        dim_head: 32
        dropout: 0.3
      height_att_config: 
        # number of encoder layers
        depth: 1
        # dimension of the input feature
        dim: 256
        # epsilon for LayerNorm
        eps: 1e-05
        heads: 8
        dim_head: 32
        dropout: 0.3
        bias: True
      width_att_config: 
        # number of encoder layers
        depth: 1
        # dimension of the input feature
        dim: 256
        # epsilon for LayerNorm
        eps: 1e-05
        heads: 8
        dim_head: 32
        dropout: 0.3
        bias: True
      # feedforward condition
      feed_forward: &feed_forward
        # dimension of the input feature
        mlp_dim: 256
        # dropout rate
        dropout: 0.3
        # bias
        bias: True
        # expansion factor
        ffn_expansion_factor: 2.66
      
kd_flag:
  teacher_model: point_pillar_disconet_teacher
  teacher_model_config: *model_args
  teacher_path: "afformer/logs/dairv2x/pointpillar_early_multiscale/2025_05_20_15_56_05/net_epoch194.pth"

loss:
  # core_method: point_pillar_tomv_loss
  core_method: point_pillar_loss
  args:
    pos_cls_weight: 2.0
    cls:
      type: 'SigmoidFocalLoss'
      alpha: 0.25
      gamma: 2.0
      weight: 2.0
    reg:
      type: 'WeightedSmoothL1Loss'
      sigma: 3.0
      codewise: true
      weight: 2.0
    dir:
      type: 'WeightedSoftmaxClassificationLoss'
      weight: 0.2
      args: *dir_args
    kd:
      weight: 10000 
      decoder_kd: true

optimizer:
  core_method: Adam
  lr: 0.001
  args:
    eps: 1e-10
    weight_decay: 1e-4

lr_scheduler:
  core_method: multistep #step, multistep and Exponential support, warmup, cosine
  gamma: 0.1
  step_size: [15, 20, 30, 50]